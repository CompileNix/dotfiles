# vim: sw=4 et

user-agent = "Mozilla/5.0 (Linux; rv:102.0) Gecko/20100101 Firefox/102.0"

# When following a redirect, automatically set the previous URL as referer.
referer = ";auto"

# Wait N seconds before timing out.
connect-timeout = 10

